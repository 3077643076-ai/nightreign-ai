# Game AI Agent — 黑夜君临训练路线图

## 目标

训练一个面向 PVE / 离线 / 练习 Mod 环境的《艾尔登法环 黑夜君临》游戏智能体。

当前毕设主线不追求一开始完整正常局通关，而是采用课程式训练：

```text
训练场夜王专项 → Boss 房纯 RL 战斗 → 爬塔连续战斗 + 装备规划 → 正常对局地图规划（后续扩展）
```

当前角色与武器约束：

- 角色：追踪者
- 武器：初始单大剑
- 第一目标：训练场刷明白所有夜王，形成稳定 Boss 战能力
- 毕设核心目标：完成训练场战斗脑与爬塔模式装备规划；正常对局作为后续扩展

## 安全与实验边界

- 仅面向 PVE、离线、练习 Mod 或个人研究环境。
- 不用于联机对抗，不破坏线上公平性。
- 公司电脑只做文档、规划和离线代码设计，不打开游戏、不运行 CE、不测试 Mod。
- 内存读取定位为训练阶段的状态采集、奖励真值和调试工具，不把“修改游戏数值”作为项目核心。
- Mod 定位为构造稳定、可重复、可重开的训练环境，例如 Boss Arena、Boss Rush、跳过非目标流程。

## 核心架构

```text
┌──────────────────────────────────────┐
│ 状态机监督器                          │
│ 死亡重开 / 加载等待 / 菜单确认 / 进场锁定 │
└───────────────────┬──────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ 指挥规划脑（低频，500-1000ms）          │
│ 下达意图：锁定 / 接近 / 防守 / 贪刀 / 喝药 │
└───────────────────┬──────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ 纯 RL 战斗脑（Boss 战高层动作选择）       │
│ 在动作库里选择：翻滚 / 攻击 / 弹反 / 喝药  │
└───────────────────┬──────────────────┘
                    ↓
┌──────────────────────────────────────┐
│ 动作执行器                            │
│ 把“向左翻滚”等高层动作翻译成键鼠/手柄输入  │
└──────────────────────────────────────┘
```

BC 不再作为主控路线。行为克隆只保留为辅助用途：动作分割参考、人类录像分析、动作库设计、论文对比实验。

## 技术栈

| 用途 | 工具 |
|------|------|
| 截图 | DXCam |
| 手柄录制 | `inputs` 库 (XInput) |
| 虚拟手柄/键鼠控制 | vgamepad / pynput |
| 深度学习 | PyTorch + CUDA |
| 强化学习 | stable-baselines3 / 自定义 Gym 环境 |
| 目标检测 | YOLOv8 / 视觉检测器 |
| OCR | Tesseract / UI 文本识别 |
| 内存状态采集 | `memory_reader.py` + 后续 CE / 解包资料验证 |
| 决策日志 | 中文 jsonl 决策轨迹 |

## 项目结构

```text
game-ai-agent/
├── recorder/              # 录制工具（截图 + 手柄 + 后续内存状态）
├── preprocess/            # 数据预处理、清洗、动作片段整理
├── perception/            # HUD / 血条 / 锁定 / 小地图 / OCR 感知
├── models/                # 模型定义（BC 对比、状态估计、RL 网络）
├── train/                 # 训练脚本
├── inference/             # 推理部署
├── planner.py             # 规划脑：规则引擎 / 装备选择 / 指挥意图
├── game_state.py          # 游戏状态解析
├── memory_reader.py       # 内存状态采集器雏形
├── data/                  # 原始录制数据（gitignore）
├── checkpoints/           # 模型权重（gitignore）
├── logs/                  # 决策轨迹与奖励日志（gitignore）
├── docs/                  # 设计文档
└── ROADMAP.md             # 当前路线图
```

## 阶段规划

### Phase 0：录制工具 ✅ 已完成

- [x] DXCam 截图 + inputs 读手柄 → 720p / 15fps / JPEG + inputs.jsonl
- [x] 验证数据：2 局 60756 帧 68 分钟，14.8fps，按键+摇杆+扳机正常
- [x] 热键启动：F8 开始 / F9 停止 / Ctrl+C 退出
- [x] 一键启动：run.bat（自动提权+依赖检查）
- [x] LT/RT 扳机二值化（>=0.5=按下）

### Phase 0.5：内存状态采集规范 🔄 规划中

目标：从录制阶段开始，同步保存画面、输入和内存真值。内存读取作为“老师”，用于奖励计算、状态标注、视觉状态估计训练和调试。

产出：

- [ ] `memory_state.jsonl`：每帧内存状态
- [ ] `metadata.json`：训练环境、角色、武器、Boss、Mod 信息
- [ ] 动作 ID / Boss ID / 装备 ID / 地点 ID 中文字典
- [ ] 地图节点坐标表（第三阶段使用）
- [ ] 中文状态摘要：方便人工回看和论文截图

第一优先级字段：

```text
玩家：hp/max_hp/hp_pct, fp/max_fp/fp_pct, stamina/max_stamina/stamina_pct
位置：pos_x, pos_y, pos_z, player_yaw
动作：player_anim_id, player_action_cn
目标：locked_on, target_hp_pct, target_anim_id, distance_to_target
流程：combat_state, in_menu, is_dead, retry_available
```

详见：`docs/memory_state_spec.md`

### Phase 0.6：状态机监督器与指挥规划脑 🔄 规划中

目标：先解决 BC/RL 不擅长的流程问题，保证 AI 不在菜单、死亡、加载界面乱按。

职责：

- [ ] 判断当前状态：加载中、菜单、死亡、再战确认、战斗中、结算/奖励
- [ ] 死亡后执行再战流程：等待 → 按 A/F → 再确认 → 等待加载
- [ ] 进入战斗后执行锁定目标
- [ ] 给战斗脑下达低频意图：接近、防守、贪刀、喝药、重开
- [ ] 限制 RL 可选动作，避免残血乱攻、Boss 抬手喝药、菜单乱按
- [ ] 输出中文规划日志

### Phase 1：训练场夜王专项 — Boss 房纯 RL 战斗脑 🎯 毕设核心

目标：使用初始单大剑，在训练场 / Boss Arena 中刷明白所有夜王。

约束：

- 不训练跑图
- 不训练装备路线
- 不依赖大规模 BC 演示素材
- 状态机负责重开、加载、锁定
- RL 只负责 Boss 战中选择高层动作

动作库第一版：

```text
锁定、接近、后撤、左绕、右绕、左翻滚、右翻滚、轻攻击、重攻击、战技、弹反、喝药、处决
```

奖励方向：

- 正奖励：造成伤害、DPS、成功规避攻击、成功弹反、处决、击杀、保持有效距离
- 负奖励：受到伤害、死亡、空挥、乱滚、低耐力攻击、无意义喝药、远离 Boss 太久

### Phase 2：中文决策轨迹与奖励分析 🎯 毕设核心

目标：让人工能看懂 AI 为什么这么打、每个动作和反馈打多少分。

产出：

- [ ] `logs/combat_trace_*.jsonl`：战斗脑决策轨迹
- [ ] `logs/planner_trace_*.jsonl`：规划脑意图与流程决策
- [ ] `logs/reward_trace_*.jsonl`：奖励函数分项
- [ ] 终端中文摘要
- [ ] 离线回放统计脚本：死亡原因、乱滚率、弹反成功率、喝药时机、DPS

详见：`docs/decision_trace_design.md`

### Phase 3：爬塔模式 — 连续战斗 + 装备规划脑 🎯 毕设争取目标

目标：接入练习场 Mod 的爬塔 / Boss Rush 模式，不规划路线，只规划装备，并点击下一场战斗。

特点：

- 不需要地图路线规划
- 使用已训练的 RL 战斗脑连续打怪
- 规划脑负责装备、遗物、奖励选择
- 状态机负责下一场战斗、结算、重开、加载

装备规划脑输入：

```text
当前武器、当前遗物、当前属性、当前血量/资源、可选奖励、下一场敌人信息
```

装备评分原则第一版：

```text
单大剑相关：近战攻击、战技攻击、满血攻击、生命/减伤、耐力收益高
无关词条：智力、魔力、非当前流派加成收益低
```

### Phase 4：正常 PVE 对局地图规划 🧭 后续扩展

目标：将战斗脑和装备规划脑迁移到正常远征。

该阶段才处理：

- 跑图
- 地图节点
- 黑夜圈
- 开地图定位
- 背包/装备 OCR
- 行动路径复盘
- 路线偏移纠正
- 卡墙/卡住恢复

需要从录制阶段保留的位置数据：

```text
pos_x/y/z, player_yaw, area_id, area_name_cn, nearest_waypoint, target_waypoint, distance_to_waypoint, stuck_score
```

第三阶段路线建议采用 `Memory Teacher → Vision Student`：

```text
先用内存坐标当真值 → 训练视觉/地图状态估计器 → 双轨对比误差 → 逐步减少内存依赖
```

### Phase 5：队友模式 / 迁移其他游戏 🔮 长期扩展

- 队友模式：救人、跟随、不抢装备、联机行为约束
- 迁移游戏：怪猎、死亡细胞、Minecraft 等
- 该阶段不纳入当前毕设核心范围

## 毕设完成标准

最低可交付：

- [ ] 状态机能处理死亡重开、加载等待、战斗进入、锁定流程
- [ ] Boss 房纯 RL 战斗脑能在训练场稳定学习至少一个夜王
- [ ] 内存状态能记录到 `memory_state.jsonl`
- [ ] 中文决策日志能解释动作选择和奖励分项

理想可交付：

- [ ] 单大剑训练场刷明白多个夜王
- [ ] 爬塔模式连续战斗可运行
- [ ] 装备规划脑能根据大剑流派选择奖励
- [ ] 输出中文训练报告：胜率、DPS、死亡原因、动作收益

后续工作：

- 正常远征地图规划
- 地图/背包/OCR 与内存坐标蒸馏
- 路线规划脑
- 完整正常局通关

## 行为克隆与公开视频的新定位

原始思路曾考虑 BC 和公开视频/VPT，但当前降级为辅助路线。

原因：

- 公开视频缺少真实按键、摇杆力度、血量、耐力、坐标和奖励反馈。
- BC 需要大量高质量素材，且容易复制失误、乱走、不锁定、卡墙、死亡后不会重开。
- Boss 房可自动重开、奖励清楚，更适合纯 RL 学习高收益打法。

保留用途：

- 动作库设计参考
- Boss 招式观察
- 人类打法分析
- BC 对比实验
- 论文中说明路线调整依据

## 训练原则

- 先解决战斗，再解决装备，最后解决正常局地图。
- 非战斗流程用状态机，不交给 BC/RL 硬学。
- 战斗脑采用纯 RL 高层动作选择，不直接输出每帧底层按键。
- 规划脑低频指挥，限制 RL 动作范围，保证流程和意图正确。
- 内存读取作为训练阶段老师：奖励真值、状态标注、视觉估计训练和调试。
- 日志必须中文可读：字段名可英文，状态摘要、动作名、原因、奖励解释用中文。
- 公司电脑只做文档与离线开发，不运行游戏、不运行 CE、不测试 Mod。

## 参考资料

### 已下载论文（X:\dev\ai_books\）

- CSGO_BC_2021.pdf — CS:GO Behavioural Cloning，作为 BC 对比与局限参考
- DarkSouls_Combat_2025.pdf — 纯像素训 AI 打魂类，作为 Boss 战训练参考
- 强化学习导论_第二版_中文版.pdf
- 深度学习_花书_中文版.pdf

### 必看项目与资料

| 项目 | 看点 |
|------|------|
| Unity ML-Agents | PPO、模仿学习、自博弈、奖励设计参考 |
| CS:GO Behavioural Cloning | 录制→训练→部署流水线，作为对比基线 |
| NVIDIA NitroGen | 大规模视觉→控制项目，作为长期参考 |
| Boss Arena / Boss Rush 类 Mod | 构造可重复 Boss 战训练场 |
| CE 表 / 解包资料 / Param 资料 | 状态字段、ID 字典、动作/装备/地图资料确认 |
